# Acrylique — assets galerie

Fichiers utilisés par le portfolio (`category`: `acrylique` dans `portfolio_images.json`).

Pour ajouter une œuvre : déposer le fichier ici + nouvelle entrée dans le JSON.
