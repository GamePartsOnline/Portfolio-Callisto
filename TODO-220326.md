# 📝 To-Do List : Optimisation Portfolio Callisto Arts

## Feuille de route (où on en est)

| # | Thème | Statut |
|---|--------|--------|
| 1 | Particules (moins, mobile off, `prefers-reduced-motion`) | ✅ Fait |
| 2 | Images (WebP/AVIF, lazy, miniatures) | ✅ Pipeline WebP + thumbs + repli JS ; lancer `scripts/generate_image_derivatives.py` après ajouts |
| 3 | Mobile (flex, nav tactile, tests navigateurs) | 🔄 Zones tactiles + safe-area + scroll ancres — tests navigateurs réels |
| 4 | Accessibilité (contrastes, `alt`, clavier) | 🔄 Base OK — audit Lighthouse / contrastes filtres & timeline à re-valider |
| 5 | Légal (cookies, mentions, contact) | 🔄 Bandeau cookies + mentions — formulaire contact backend si besoin |
| 6 | Modernisation (Vite, nettoyage CSS) | ⏳ Plus tard |

**Anti-patterns** (rappel) : pas d’autoplay audio sans action utilisateur ✅ (lecteur avec bouton) · pas de scroll hijacking ✅ · texte dans les images à éviter.

---

## 🚀 Performance & Technique

### Optimisation des images

- [x] Pipeline WebP + miniatures : `scripts/generate_image_derivatives.py` + chemins `assets/images/thumbs/` et `assets/images/webp/` (voir `docs/GUIDE.md`).
- [x] Lazy Loading (`loading="lazy"`) sur les images générées par la grille portfolio (`script.js`).
- [x] Miniatures dédiées pour la grille (`*-thumb.webp`, repli vers fichier d’origine si absent).

### Gestion des particules

- [x] Réduire le nombre de points (desktop ~28, pas de « connexions » — particules seules).
- [x] Désactiver les particules sur mobile (`max-width: 767px`).
- [x] Respecter `prefers-reduced-motion` (points statiques réduits sur desktop).

### Modernisation

- [ ] Migrer le projet vers Vite.js (ou Next.js si besoin d’une DB).
- [ ] Nettoyer le CSS (redondances, variables déjà en partie dans `styles.css`).

---

## 📱 Ergonomie & Responsive

### Adaptation Mobile

- [x] Corriger les débordements / scroll horizontal (html + `#main-content`, etc.) — à re-valider après gros changements.
- [x] Navigation tactile : `--touch-target-min` (44px), menu burger / liens / filtres / hero / cookies ; `safe-area-inset` ; `scroll-padding-top` ; menu mobile scrollable.
- [ ] Tests sur plusieurs navigateurs / tailles d’écran (Safari iOS, Chrome Android).

---

## ⚖️ Accessibilité & Légalité (WCAG / RGPD)

### Accessibilité (WCAG)

- [ ] Re-valider les contrastes (Lighthouse / axe) après changements CSS — filtres par catégorie, timeline Journey.
- [x] `alt` descriptifs générés pour grille, hero et lightbox (`buildArtworkAltText` dans `script.js`) ; logos décoratifs avec `alt=""`.
- [x] Lien « Aller au contenu principal » + `tabindex="-1"` sur `<main>` + focus après navigation.
- [x] Focus visible (`:focus-visible`) : menu burger, liens nav, filtres portfolio, cartes œuvres ; `aria-controls` sur le bouton menu.
- [x] Renfort contrastes : `--color-text-tertiary`, footer, mentions légales, lecteur audio (labels / temps).

### Conformité Légale

- [x] Bannière cookies (UE) : `assets/js/cookie-consent.js` + bandeau fixe, `localStorage`, lien « Réglages cookies » + section `#cookies` dans les mentions.
- [x] Page Mentions légales (`mentions-legales.html`) + lien dans le pied de page.
- [ ] Formulaire de contact sécurisé (si backend / endpoint).

---

## 🚩 Choses à ÉVITER (Anti-patterns)

- Ne pas utiliser d’**Autoplay Audio** sans interaction — le lecteur actuel demande un clic ✅
- Ne pas faire de **Scroll Hijacking**.
- Éviter le **texte incrusté dans les images** (préférer HTML + styles).

---

## Prochaine action suggérée

1. ~~**Images**~~ · ~~**Mentions**~~ · ~~**A11y clavier (base)**~~
2. ~~**Cookies**~~ : bannière + mentions — affiner si vous ajoutez Matomo/GA ou bloquer YouTube avant consentement.
3. **Ou** **A11y** : audit contrastes + `alt` descriptifs sur les visuels.
4. **Ou** **Mobile** : tailles des zones tactiles, menu burger re-test.

---

_Dernière mise à jour : 22 mars 2026_
